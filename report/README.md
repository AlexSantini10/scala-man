# Report

This directory contains the LaTeX source for the final report.

The entry point is `main.tex`, which includes the chapter files from
`chapters/`.

To build the report locally:

```bash
make
```

This generates `report.pdf` from `main.tex` and keeps the temporary LaTeX
files out of version control.
